/**
 * A type that represents data that is required for any Flowchart component
 */
export default interface FlowchartPrimitive {
    x: number;
    y: number;
    width: number;
    height: number;
}